def print_matrix_integer(matrix=[[]]):
    for row in matrix:
        for i, d in enumerate(row):
            if i == len(row) - 1
                print("{:d}".format(d))
            else:
                print("{:d}".format(d), end=' ')
    if matrix == [[]]
        print()
